# Prior prototype: migration reference only

Source: https://github.com/BrownBOBAsushi/Dattebayo/tree/f7ce99f13b0eab5146afec1a047babeb034ab3c1
Snapshot commit: `f7ce99f13b0eab5146afec1a047babeb034ab3c1`.

This is Edmund's practice prototype and the original diagnostic probe, not the new battle implementation. Reuse the CV preprocessing, sequence logic, model loading and audio handling selectively. Source files are unchanged from the pinned commit. No Git history, hosting identity or credentials are included.

Run `npm test` in this folder for its existing tests. Browser model binaries and libraries are fetched from URLs in the code; they are not bundled. Local model loading was reported working by the user; broad gesture reliability and public hosted camera behavior remain unverified.

Existing audio and art are optional reference assets. Preserve `assets/CREDITS.md` and `assets/audio/SOURCES.md`. Availability is not a redistribution license; do not automatically publish the bundled reference folder. Generate the new runtime art and original procedural audio as the architecture specifies.
