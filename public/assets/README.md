# Runtime art manifest

The first playable build intentionally renders a clearly labelled procedural
greybox. `manifest.json` is the complete fixed-facing inventory for the
approved GPT-image-2.5 art pass. Every path is relative and marked
`pending-gpt-image-2.5`; no supplied handoff image or legacy Naruto media is
used as runtime art.
